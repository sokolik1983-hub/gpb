/** Тип счета. */
export interface AccountType {
  /** Код типа счета. */
  code: number;
  /** Краткое наименование. */
  description: string;
  /** Полное наименование. */
  fullDescription: string;
}
