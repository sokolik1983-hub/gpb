/** Свойства подразделения обслуживания. */
export interface ServiceBranch {
  /** Код филиала в РКО Ф1. */
  absNumber: string;
  /** Название филиала. */
  filialName: string;
  /** Идентификатор. */
  id: string;
}
