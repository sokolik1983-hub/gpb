/* eslint-disable @typescript-eslint/no-var-requires */
import { createIcon } from '@platform/ui';

/** Спиннер скроллера. */
export const ScrollerSpinnerIcon = createIcon(require('./assets/spinner.svg'));

/** Плейсхолдера скроллера оборотов. */
export const TurnoverScrollerPlaceholderIcon = createIcon(require('./assets/turnover-scroller-placeholder.svg'));
