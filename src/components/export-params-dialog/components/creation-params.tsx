import React from 'react';
import { Box } from '@platform/ui';

/** Компонент параметров запроса. */
export const CreationParams: React.FC = () => <Box>CreationParams</Box>;

CreationParams.displayName = 'CreationParams';
