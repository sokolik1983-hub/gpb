import React from 'react';
import { Box } from '@platform/ui';

/** Компонент параметров комплекта документов. */
export const DocumentsParams: React.FC = () => <Box>DocumentsParams</Box>;

DocumentsParams.displayName = 'DocumentsParams';
