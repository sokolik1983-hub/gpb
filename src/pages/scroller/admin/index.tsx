import React from 'react';
import { locale } from 'localization';
import { Box } from '@platform/ui';

export const AdminScrollerPage = () => <Box>{locale.admin.scroller.title}</Box>;

AdminScrollerPage.displayName = 'AdminScrollerPage';
