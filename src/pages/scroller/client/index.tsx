import React from 'react';
import { locale } from 'localization';
import { Box } from '@platform/ui';

export const ClientScrollerPage = () => <Box>{locale.client.scroller.title}</Box>;

ClientScrollerPage.displayName = 'ClientScrollerPage';
