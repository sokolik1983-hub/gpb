/** Формат даты фильтров скроллера. */
export const DATE_ISO_FORMAT = 'YYYY-MM-DD';

/** Используется для значения выбранного в селекте, когда в нём не выбрано значение. */
export const EMPTY_VALUE = '';

/** Параметр урла обозначающий идентификатор сущности. */
export const ID_URL_PARAMETER = ':id';

/** Значение параметра урла, при котором форма открывается для создания нового запроса на выписку. */
export const NEW_ENTITY_ID = 'new';

/** Наименование стрима. */
export const ECO_STATEMENT = '@eco/statement';
